# ============================================================
# Core agent framework (start with CrewAI for MVP)
# ============================================================
crewai>=0.28.0
anthropic>=0.40.0
# Phase 5 Portfolio Manager + Execution Agent: Claude Agent SDK for MCP tool-calling
# (portfolio manager gets a read-only Alpaca MCP tool; execution agent dispatches
# through a custom guarded MCP tool). Bundles the mcp Python SDK as a dependency.
claude-agent-sdk>=0.2.0

# ============================================================
# Database
# ============================================================
supabase>=2.0.0

# ============================================================
# Game data APIs
# ============================================================
# Steam: uses requests directly (no official SDK)
requests>=2.31.0
# IGDB: official Python wrapper
igdb-api-v4>=0.2.0
# RAWG: uses requests directly

# ============================================================
# Sentiment (Phase 2)
# ============================================================
vaderSentiment>=3.3.2  # VADER baseline sentiment scorer
playwright>=1.40.0     # Low-volume studio careers fallback where hosted ATS APIs are unavailable
# Reddit alternate-egress adapters (agents/workers/sentiment/reddit_source.py) use
# only the already-pinned `requests` above -- no new dependency. The OAuth path
# deliberately avoids `praw` (matches this repo's no-SDK convention, e.g.
# alpaca_trading_client.py / email_delivery.py); the proxy path is HTTP/HTTPS only
# (no `PySocks`/SOCKS5 support, and none needed).

# ============================================================
# Financial data (Phase 3)
# ============================================================
yfinance>=0.2.36
sec-edgar-downloader>=5.0.0

# ============================================================
# Observability (Phase 4)
# ============================================================
langsmith>=0.1.0

# ============================================================
# Utilities
# ============================================================
python-dotenv>=1.0.0   # load .env files
pydantic>=2.0.0        # structured output validation
httpx>=0.25.0          # async HTTP

# ============================================================
# Testing
# ============================================================
pytest>=8.0.0           # unit tests (see tests/)
